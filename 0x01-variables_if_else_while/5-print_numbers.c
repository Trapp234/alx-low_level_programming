#include <stdio.h>

/**
 * main - prints numbers from 0 -9
 *
 * Return: (0) Always
 */

int main(void)
{
	int i = 0;

	while (i <= 9)
	{
		printf("%d", i);
			i++;
	}
		printf("\n");
		return (0);
}
